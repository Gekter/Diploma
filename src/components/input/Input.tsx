import classNames from "classnames";
import React from "react";
import styles from "./Input.module.css";
import { Input } from "@chakra-ui/react";

const MyInput = (props: any) => {
  return <Input {...props} va />;
};

export default MyInput;
